VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Google Fit — OAuth Client ID (Google Cloud Console → APIs & Services →
# Credentials → OAuth 2.0 Client ID, type "Web application").
# Add your app's origin (e.g. http://localhost:5173) to Authorized JavaScript
# origins. Required for the Health Sync page's "Connect Google Fit" button.
VITE_GOOGLE_FIT_CLIENT_ID=