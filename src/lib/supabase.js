/**
 * Backwards-compatible re-export.
 * The canonical client lives in supabaseClient.js — import from there
 * or from here, both work identically.
 */
export { supabase, default } from './supabaseClient'
